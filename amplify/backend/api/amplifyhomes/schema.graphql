type Drink @model @auth(rules: [{allow: public}]) {
  id: ID!
  Name: String
  Dedscription: String
  Star: Int
  Price: Int
  ImageUrl: String
}

type Home @model @auth(rules: [{allow: public}]) {
  id: ID!
  address: String
  image_url: String
  price: Float
}
 